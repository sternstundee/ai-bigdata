import math


def fun_calc_triangle_area(a, b, c):
    """
    自定义函数：计算三角形面积（海伦公式）
    参数：a, b, c（三角形三边长，通过关键字参数传递）
    返回：三角形面积（float）
    """
    p = (a + b + c) / 2  # 半周长
    area = math.sqrt(p * (p - a) * (p - b) * (p - c))  # 海伦公式
    return round(area, 2)  # 保留2位小数


# 主调程序：支持计算多个三角形
while True:
    try:
        # 输入三边长（确保为正数）
        a = float(input("\n请输入三角形第一条边长（正数）："))
        b = float(input("请输入三角形第二条边长（正数）："))
        c = float(input("请输入三角形第三条边长（正数）："))

        # 校验边长为正数
        if a <= 0 or b <= 0 or c <= 0:
            print("❌ 边长必须为正数，请重新输入！")
            continue

        # 判断能否构成三角形（任意两边之和>第三边）
        if (a + b > c) and (a + c > b) and (b + c > a):
            # 关键字参数传递，调用函数计算面积
            area = fun_calc_triangle_area(a=a, b=b, c=c)
            print(f"✅ 三角形面积为：{area} 平方单位")
        else:
            print("❌ 你所输入的三边长不能构成三角形！")

        # 询问是否继续计算
        continue_flag = input("是否继续计算下一个三角形？（y/n）：").strip().lower()
        if continue_flag != "y":
            print("👋 程序结束！")
            break

    except ValueError:
        print("❌ 输入错误！请输入有效的数字（如3.5、5）。")