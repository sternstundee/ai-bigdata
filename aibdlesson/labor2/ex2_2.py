import random

try:
    # 输入班级人数n和点名人数m
    n = int(input("请输入班级人数："))
    m = int(input("请输入点名人数："))

    # 校验输入合法性（m需在1~n之间）
    if m < 1 or m > n:
        print(f"❌ 点名人数需在1~{n}之间，请重新输入！")
    else:
        # 生成m个互不重复的随机学号（1~n）
        selected_ids = random.sample(range(1, n + 1), m)
        # 格式化输出（补零为2位，如1→01）
        print("\n📢 随机点名结果（学号）：")
        print("、".join([f"{id:02d}" for id in selected_ids]))

except ValueError:
    print("❌ 输入错误！请输入正整数。")